[pytest]
addopts = -q --alluredir=allure-results
testpaths = tests
markers =
    smoke: critical smoke tests
    regression: full regression suite
    login: authorization/login tests
    negative: negative scenarios
    slow: slow or timing-sensitive tests